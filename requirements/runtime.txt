# What the service needs to run, as ranges a person reads. The image installs
# requirements/runtime.lock, which pins all of it with hashes — after changing
# this file, run `bash scripts/lock_requirements.sh` and commit both. Nothing
# here should be a development tool.
fastapi==0.115.*
uvicorn[standard]==0.34.*
jinja2==3.1.*
httpx==0.28.*
pydantic==2.11.*
pydantic-settings==2.8.*
python-multipart>=0.0.9
user-agents==2.2.*
# Renders docs/*.md for the dashboard's /docs pages. Chosen over
# Python-Markdown for one property: it can be told to escape raw HTML
# (html=False) instead of passing it into a page built around a strict
# CSP. Only transitive dependency is mdurl.
markdown-it-py==4.2.*
